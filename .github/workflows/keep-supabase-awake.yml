name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 0 */1 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl --max-time 10 "https://hpjnzbutlpbqyhyvfbtp.supabase.co/auth/v1/health"
